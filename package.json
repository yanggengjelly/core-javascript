{
  "dependencies": {
    "live-server": "^1.2.2"
  },
  "scripts": {
    "dev": "live-server client --host=localhost --port=5500 --no-browser",
    "serve": "json-server server/data/data.json",
    "all": "run-p dev serve"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "json-server": "^0.17.4",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.2.4"
  }
}
